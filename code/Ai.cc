#include "Ai.h"
#include "Move.h"

#include <vector>
#include <utility>

using namespace std;

Move Ai::getMove(Game *g)
{
	for (auto i: )



	return Move(make_pair(0, 0), make_pair(0, 0), 0);
}

Ai::Ai(int lvl, vector< pair<int, int> > myPieces): lvl{lvl}, myPieces{myPieces}
{
	
}
